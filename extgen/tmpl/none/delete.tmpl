// DELETE {{.Path}}
func (c *LingioClient) {{.FuncName}}(ctx context.Context, {{.Params}}) (*{{.RetObjType}}, error) {
	var x {{.RetObjType}}

    {{- if .QueryParams}}
    // Handle query parameters
	v := url.Values{}

	{{- range .QueryParams}}

	// {{ .Name }} of type {{ .Type }}
	{{if .Required }}
    v.Set("{{.Name}}", fmt.Sprintf("{{. | Format}}", {{ .Name }}))
    {{else}}
	if {{.Name}} != nil {
    	v.Set("{{.Name}}", fmt.Sprintf("{{. | Format}}", *{{ .Name }}))
	}
	{{end}}

    {{- end}}

	b, err := common.HttpDelete(ctx, fmt.Sprintf("%s{{.PathTemplate}}?%s", c.Server, {{.Params2}}, v.Encode()), "")
	{{- else}}
	b, err := common.HttpDelete(ctx, fmt.Sprintf("%s{{.PathTemplate}}", c.Server, {{.Params2}}), "")
	{{- end}}
    {{template "parseJson.tmpl" -}}
}


